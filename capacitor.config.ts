import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.erikmota.workforceportal',
  appName: 'Workforce Portal - ERIK MOTA',
  webDir: 'dist/browser',
  server: {
    androidScheme: 'https',
  },
};

export default config;
